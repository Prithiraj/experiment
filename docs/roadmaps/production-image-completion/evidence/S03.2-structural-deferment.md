# S03.2 evidence — Structural component deferment

The approved integration uses four complete flower states rather than constructing a bloom from independently hinged raster petals. An independent stem already exists in CSS, and no macro transition requiring separate sepals, stamens, center, seed pod, or pollen has been approved for this iteration.

Generating these parts now would add files, manifest surface, downloads, and visual continuity risk without an owning scene. The candidate ledger retains stable basenames so they can be reopened when a concrete component animation or macro chapter is approved.

**Verdict:** `S03.2a` and `S03.2b` are **DEFERRED** by accepted scope decision, satisfying parent `S03.2` for this iteration.
