# S01 evidence — Ledger review

**Reviewed:** 2026-09-10  
**Ledger:** `assets/reference/production-candidate-inventory.csv`

## Source coverage

- 21 source rows cover IDs `00` through `20` exactly once.
- `01` and `02` are the only direct-convert sources.
- `03`–`17` are contact-sheet references, not production files.
- `00` and `18`–`20` are locked reference-only.

## Candidate consolidation

Fifteen contact sheets repeat the same flower states, structural parts, petal families, leaf angles, water plates, and atmosphere elements. Representative full-board review showed compact labeled panels and either black or baked checkerboard backgrounds. Their per-panel resolution is below the 1200 × 1200 cutout target, so none is approved for direct extraction.

The ledger consolidates those appearances into canonical candidates and assigns one action per candidate. Required new work is intentionally limited to:

1. early-open lotus;
2. half-open lotus;
3. water reflection plate;
4. water ripple overlay.

Existing bud, full bloom, loose petals, foreground leaf, water plate, and bouquet assets are retained. Structural/macro/depth/atmosphere candidates without an approved scene need are deferred rather than generated speculatively.

## Verdict

`S01.1`, `S01.2`, and `S01.3` are **VERIFIED**. `S01` is **VERIFIED** and releases `S02` and the approved `S03` generation children.
