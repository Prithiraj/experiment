# S05.2 evidence — Responsive cinematic handoff

A new `bloom` scene sits between the release hero and zero-gravity field. Its pinned full-motion timeline moves through:

1. closed-bud pond scene;
2. water reflection plus restrained screen-blend ripple;
3. full-bloom pond scene.

The scene uses the established GSAP orchestrator and returns a scoped cleanup. The new navigation target `#story` opens this authored chapter rather than the later axis section, and gravity telemetry now includes the bloom transition.

## Responsive delivery

All four plates declare `768w` and `1536w` WebP sources with `sizes="100vw"`. Browser verification at 390 × 844 selected every 768 px derivative; the default desktop viewport selected every 1536 px derivative. Initial page-load inspection confirmed that none of the chapter sources is assigned at scroll position zero; all four load only as the chapter approaches.

## Visual review

- Desktop start, mid-reflection, and full-bloom end states retain readable copy and intentional central crops.
- The ripple composites without a rectangular matte.
- The 390 × 844 crop keeps the bud/bloom center, title, body copy, and stage index legible.
- The chapter background remains dark and stable if an image is briefly unavailable.

**Verdict:** `S05.2` is **VERIFIED**.
