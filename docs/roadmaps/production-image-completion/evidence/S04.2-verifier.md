# S04.2 evidence — Asset verifier

`scripts/verify-assets.mjs` performs deterministic repository checks without adding a package dependency.

## Verified contracts

- PNG/WebP container validity, dimensions, and alpha-channel expectation.
- Exact byte size and SHA-256 for every production master and derivative.
- Per-derivative payload budget.
- Unique manifest IDs and paths, lifecycle status, provenance, role, and scene ownership.
- Local source hashes through `npm run verify:assets:local`.
- Runtime key/path parity across the manifest and `src/assets.ts`.

## Run result

`npm run verify:assets:local` passed with 17 assets, 38 files, 3,207,498 total derivative bytes, and 11 runtime keys. The normal `npm run check` now runs the portable asset verifier before TypeScript and also passed.

**Verdict:** `S04.2` is **VERIFIED**.
