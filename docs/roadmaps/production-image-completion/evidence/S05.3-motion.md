# S05.3 evidence — Lotus component motion states

The release hero and cinematic bloom chapter now use the same four-state progression:

`closed bud → early-open → half-open → full bloom`

## Implementation

- The hero loads the closed bud as critical, the early-open state as the only immediate next-state preload, then loads half-open and full-bloom sequentially.
- Whole-flower states are stacked in one stable layout box and animated with opacity/scale only; no layout property is animated.
- The bloom chapter introduces each cutout over the reflection plate, then hands the final cutout into the matching full-bloom cinematic scene.
- Short non-overlapping fades avoid a prolonged double-flower silhouette while preserving reversible scrub behavior.
- Repeated scene actors share the same imported URLs and browser cache entries.

## Browser review

At the desktop test viewport, sampled hero positions resolved to the expected dominant states: bud at entry, early-open in the first transition, half-open at mid-scroll, and full bloom before scene exit. The chapter loaded all four deferred cutouts only near its viewport and displayed them in the same order over the water plate.

All 17 manifest assets now have verified runtime mappings, and application HTML references only declared keys.

**Verdict:** `S05.3` is **VERIFIED**.
