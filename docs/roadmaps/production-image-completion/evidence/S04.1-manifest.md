# S04.1 evidence — Manifest schema

`assets/production/manifest.json` is now schema version 2 with 17 asset records and 38 explicitly owned image files.

Each record includes a stable ID, category, semantic role, lifecycle status, source provenance, owning scenes, PNG master metadata, optimized derivative metadata, dimensions, alpha expectation, exact bytes, SHA-256, and derivative budget. Archive-direct cinematics retain archive IDs and source hashes; new generated assets point to their preserved generation sources. Runtime-integrated records also declare the corresponding application key and WebP path.

Manifest paths and the actual production image count both resolve to 38, with no duplicate IDs or paths.

**Verdict:** `S04.1` is **VERIFIED**.
