# S00.2 evidence — Reference/production boundary

**Checked:** 2026-09-10

## Results

- `.gitignore` excludes `/zero_g_bloom_all_generated_images.zip`.
- `.gitignore` excludes `/assets/reference/conversation-archive-originals/`.
- Git tracks only the 21 small reference WebPs plus reference README/inventory in the archive preview folder.
- Runtime source contains no import from `assets/reference/`, no contact-sheet filename, and no broad-concept filename.
- Production runtime imports resolve only below `assets/production/`.

## Verdict

`S00.2` is **VERIFIED**. The source archive is preserved locally and isolated from application delivery.
