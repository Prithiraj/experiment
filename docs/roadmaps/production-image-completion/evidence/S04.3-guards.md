# S04.3 evidence — Reference and orphan guards

The verifier cross-checks every PNG, WebP, or AVIF below `assets/production/` against exactly one manifest file record. It rejects unmanifested production images, missing manifest files, duplicate paths, non-optimized runtime imports, runtime keys absent from either the manifest or `productionAssets`, and unknown `data-production-image` keys.

Runtime source scanning rejects reference-tree paths and tokens associated with conversation archives, contact sheets, visual-direction boards, or the mistaken homepage concept. The current repository passes with zero orphan files, zero unknown bindings, and zero prohibited reference imports.

**Verdict:** `S04.3` is **VERIFIED**.
