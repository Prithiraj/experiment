# S00.2 — Reference/production boundary

**Objective:** Keep archive originals and visual boards isolated from application delivery.

**In scope:** `.gitignore`, tracked-file review, source imports, production paths.

**Invariant:** Only approved optimized files under `assets/production/` may be imported by application code.

## Acceptance

- ZIP and extracted-original folders are ignored.
- No `assets/reference/` path appears in runtime source imports.
- `00` and `18`–`20` are explicitly locked as reference-only in the ledger.
- Results are recorded in `evidence/S00.2-boundary.md`.
