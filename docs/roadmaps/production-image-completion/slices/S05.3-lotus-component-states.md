# S05.3 — Lotus component states

**Objective:** Use approved intermediate lotus/component assets to make bloom progression legible without overloading the chapter.

## Acceptance

- Closed, early-open, half-open, and full-bloom states appear in order.
- Component transforms use stable origins and avoid layout-driven animation.
- The implementation does not introduce duplicate downloads for repeated states.
- Decorative images remain semantically hidden; meaningful content is not image-only.
