# S00.1 — Archive integrity

**Objective:** Prove the ZIP has been extracted completely and every original is unchanged.

**In scope:** expected filenames, file count, non-zero size, PNG readability, dimensions, SHA-256 comparison.

**Out of scope:** image optimization or visual selection.

## Acceptance

- Exactly 21 expected PNGs are present.
- Every SHA-256 matches `assets/reference/conversation-archive/asset_inventory.csv`.
- Every file decodes as a valid sRGB PNG.
- Results are recorded in `evidence/S00.1-archive-integrity.md`.
