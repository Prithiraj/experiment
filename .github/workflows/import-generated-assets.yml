name: Import generated image archive

on:
  push:
    branches: [main]
    paths:
      - '.asset-staging/zero-g-bloom-assets.tar.gz.b64'

permissions:
  contents: write

jobs:
  import-assets:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Decode and extract archive
        run: |
          mkdir -p assets/generated/conversation-archive
          base64 -d .asset-staging/zero-g-bloom-assets.tar.gz.b64 > /tmp/zero-g-bloom-assets.tar.gz
          tar -xzf /tmp/zero-g-bloom-assets.tar.gz -C assets/generated/conversation-archive
          rm .asset-staging/zero-g-bloom-assets.tar.gz.b64

      - name: Commit generated assets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add assets/generated/conversation-archive .asset-staging/zero-g-bloom-assets.tar.gz.b64
          git commit -m "assets: archive generated Zero-G Bloom image references"
          git push
